'use strict';

describe('myApp.graphView module', function() {

  beforeEach(module('myApp.graphView'));

  describe('GraphView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var graphViewCtrl = $controller('GraphViewController');
      expect(graphViewCtrl).toBeDefined();
    }));

  });
});