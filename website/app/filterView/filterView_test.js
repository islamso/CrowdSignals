'use strict';

describe('myApp.filterView module', function() {

  beforeEach(module('myApp.filterView'));

  describe('FilterView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var filterViewCtrl = $controller('FilterViewController');
      expect(filterViewCtrl).toBeDefined();
    }));

  });
});